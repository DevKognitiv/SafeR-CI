import 'package:flutter/material.dart';

/// Placeholder — replaced by the BrandCatalogScreen workstream.
class BrandCatalogScreen extends StatelessWidget {
  const BrandCatalogScreen({super.key});

  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('BrandCatalogScreen')), body: const Center(child: Text('BrandCatalogScreen (à implémenter)')));
}
