import 'package:flutter/material.dart';

/// Placeholder — replaced by the QrScanScreen workstream.
class QrScanScreen extends StatelessWidget {
  const QrScanScreen({super.key});

  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('QrScanScreen')), body: const Center(child: Text('QrScanScreen (à implémenter)')));
}
