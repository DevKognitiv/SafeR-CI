import 'package:flutter/material.dart';

/// Placeholder — replaced by the HomeManagementScreen workstream.
class HomeManagementScreen extends StatelessWidget {
  const HomeManagementScreen({super.key});

  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('HomeManagementScreen')), body: const Center(child: Text('HomeManagementScreen (à implémenter)')));
}
