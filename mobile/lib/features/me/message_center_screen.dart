import 'package:flutter/material.dart';

/// Placeholder — replaced by the MessageCenterScreen workstream.
class MessageCenterScreen extends StatelessWidget {
  const MessageCenterScreen({super.key, this.initialKind});

  final String? initialKind;

  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('MessageCenterScreen')), body: const Center(child: Text('MessageCenterScreen (à implémenter)')));
}
