import 'package:flutter/material.dart';

/// Placeholder — replaced by the MeScreen workstream.
class MeScreen extends StatelessWidget {
  const MeScreen({super.key});

  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('MeScreen')), body: const Center(child: Text('MeScreen (à implémenter)')));
}
