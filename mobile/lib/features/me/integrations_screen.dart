import 'package:flutter/material.dart';

/// Placeholder — replaced by the IntegrationsScreen workstream.
class IntegrationsScreen extends StatelessWidget {
  const IntegrationsScreen({super.key});

  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('IntegrationsScreen')), body: const Center(child: Text('IntegrationsScreen (à implémenter)')));
}
