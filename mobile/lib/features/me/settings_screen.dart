import 'package:flutter/material.dart';

/// Placeholder — replaced by the SettingsScreen workstream.
class SettingsScreen extends StatelessWidget {
  const SettingsScreen({super.key});

  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('SettingsScreen')), body: const Center(child: Text('SettingsScreen (à implémenter)')));
}
