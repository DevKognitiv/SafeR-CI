import 'package:flutter/material.dart';

/// Placeholder — replaced by the MembersScreen workstream.
class MembersScreen extends StatelessWidget {
  const MembersScreen({super.key, required this.homeId});

  final String homeId;

  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('MembersScreen')), body: const Center(child: Text('MembersScreen (à implémenter)')));
}
