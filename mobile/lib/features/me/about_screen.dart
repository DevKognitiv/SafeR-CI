import 'package:flutter/material.dart';

/// Placeholder — replaced by the AboutScreen workstream.
class AboutScreen extends StatelessWidget {
  const AboutScreen({super.key});

  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('AboutScreen')), body: const Center(child: Text('AboutScreen (à implémenter)')));
}
