import 'package:flutter/material.dart';

/// Placeholder — replaced by the SecurityScreen workstream.
class SecurityScreen extends StatelessWidget {
  const SecurityScreen({super.key});

  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('SecurityScreen')), body: const Center(child: Text('SecurityScreen (à implémenter)')));
}
