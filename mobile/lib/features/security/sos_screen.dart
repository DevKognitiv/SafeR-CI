import 'package:flutter/material.dart';

/// Placeholder — replaced by the SosScreen workstream.
class SosScreen extends StatelessWidget {
  const SosScreen({super.key});

  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('SosScreen')), body: const Center(child: Text('SosScreen (à implémenter)')));
}
