import 'package:flutter/material.dart';

/// Placeholder — replaced by the HomeScreen workstream.
class HomeScreen extends StatelessWidget {
  const HomeScreen({super.key});

  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('HomeScreen')), body: const Center(child: Text('HomeScreen (à implémenter)')));
}
