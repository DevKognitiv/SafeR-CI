import 'package:flutter/material.dart';

/// Placeholder — replaced by the RoomManagementScreen workstream.
class RoomManagementScreen extends StatelessWidget {
  const RoomManagementScreen({super.key});

  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('RoomManagementScreen')), body: const Center(child: Text('RoomManagementScreen (à implémenter)')));
}
