import 'package:flutter/material.dart';

/// Placeholder — replaced by the DeviceSettingsScreen workstream.
class DeviceSettingsScreen extends StatelessWidget {
  const DeviceSettingsScreen({super.key, required this.deviceId});

  final String deviceId;

  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('DeviceSettingsScreen')), body: const Center(child: Text('DeviceSettingsScreen (à implémenter)')));
}
