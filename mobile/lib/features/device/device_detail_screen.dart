import 'package:flutter/material.dart';

/// Placeholder — replaced by the DeviceDetailScreen workstream.
class DeviceDetailScreen extends StatelessWidget {
  const DeviceDetailScreen({super.key, required this.deviceId});

  final String deviceId;

  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('DeviceDetailScreen')), body: const Center(child: Text('DeviceDetailScreen (à implémenter)')));
}
