import 'package:flutter/material.dart';

/// Placeholder — replaced by the LoginScreen workstream.
class LoginScreen extends StatelessWidget {
  const LoginScreen({super.key});

  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('LoginScreen')), body: const Center(child: Text('LoginScreen (à implémenter)')));
}
