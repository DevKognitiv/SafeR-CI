import 'package:flutter/material.dart';

/// Placeholder — replaced by the RegisterScreen workstream.
class RegisterScreen extends StatelessWidget {
  const RegisterScreen({super.key});

  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('RegisterScreen')), body: const Center(child: Text('RegisterScreen (à implémenter)')));
}
