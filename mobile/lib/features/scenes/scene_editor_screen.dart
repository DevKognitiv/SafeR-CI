import 'package:flutter/material.dart';

/// Placeholder — replaced by the SceneEditorScreen workstream.
class SceneEditorScreen extends StatelessWidget {
  const SceneEditorScreen({super.key, this.sceneId});

  final String? sceneId;

  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('SceneEditorScreen')), body: const Center(child: Text('SceneEditorScreen (à implémenter)')));
}
