import 'package:flutter/material.dart';

/// Placeholder — replaced by the AutomationEditorScreen workstream.
class AutomationEditorScreen extends StatelessWidget {
  const AutomationEditorScreen({super.key, this.automationId});

  final String? automationId;

  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('AutomationEditorScreen')), body: const Center(child: Text('AutomationEditorScreen (à implémenter)')));
}
