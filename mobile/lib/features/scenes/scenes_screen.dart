import 'package:flutter/material.dart';

/// Placeholder — replaced by the ScenesScreen workstream.
class ScenesScreen extends StatelessWidget {
  const ScenesScreen({super.key, this.initialTab = 0});

  final int initialTab;

  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('ScenesScreen')), body: const Center(child: Text('ScenesScreen (à implémenter)')));
}
